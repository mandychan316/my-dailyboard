# 个人工作生活 App · 产品需求文档（PRD）

- 文档版本：v1.0
- 更新日期：2026-08-25
- 产品形态：仅在本机浏览器运行的个人本地 App（macOS）
- 目标用户：用户（仅本人使用）

---

## 1. 产品目标和使用场景

### 1.1 产品目标
做一个只供 用户 自己使用的工作生活专属 App，把每天最常打交道的几件事收进一个地方：今天的计划、三餐吃什么、运动练没练、AI 怎么用、内容做到哪一步。打开 App 就是「今天」，一眼看清，随手记录，数据永久留在自己电脑上。

### 1.2 使用场景
- 每天早上打开 App 看今天的计划、今天吃什么、今晚练什么；
- 白天随手记录：待办、备忘、AI 使用心得、内容灵感；
- 晚上打卡：运动完成、三餐吃完；
- 不定期备份数据，换电脑或重装时恢复。

### 1.3 核心原则
- 只在本机运行，不需要登录、联网、云数据库、部署、手机端；
- 数据保存在电脑上的独立文件中，刷新、关闭、重启后不丢失；
- 每个模块都有符合自身场景的功能，不是同一个任务列表换名字；
- 中文界面，操作简单顺手，改动即时自动保存。

---

## 2. 整体页面框架和导航结构

### 2.1 布局
- 左侧固定导航栏：App 名称 + 6 个板块入口 + 底部「设置」（备份 / 恢复入口）；
- 右侧为内容区，一次显示一个板块；
- 窗口变窄时导航自动收成顶部横条（响应式，向下兼容小窗口）。

### 2.2 导航结构（6 个一级页面）
1. 首页总览（Home）
2. 今日计划（Today）
3. AI实践（AI Practice）
4. 自媒体（Media）
5. 运动计划（Exercise）
6. 饮食计划（Diet）

### 2.3 视觉与交互基调（已确认）
- 配色：页面底色青白 `#F1F4F1`；文字墨绿黑 `#2C3330`；主色黛青 `#416B5D`；卡片/分隔暖沙 `#E3DCCB`；强调色落日橙 `#D97B5F`（仅用于完成、打卡、重点）。
- 字体：大标题/日期用「宋体」，正文用「苹方」，数字用等宽感。
- 交互：所有改动即时自动保存，操作后右下角轻提示「已保存」；删除一律二次确认；日期类页面统一提供「前一天 / 后一天 / 回到今天」。

---

## 3. 首页的内容结构

首页 = 「今日路线」，一条竖向时间线串起一天。

### 3.1 页面顶部
- 当前日期（如：8月25日 · 星期二）；
- 快捷备忘：输入一句话回车即存，下方显示最近几条备忘。

### 3.2 今日路线（5 个站点，按一天顺序排列）
| 站点 | 展示摘要 | 点击去向 |
| --- | --- | --- |
| 今日计划 | 完成 3/5 | 今日计划页面 |
| 三餐 | 早✓ 午✗ 晚✗ | 饮食计划页面 |
| 运动 | 今晚还没练 / 已完成打卡 | 运动计划页面 |
| AI实践 | 今天记了 2 条 | AI实践页面 |
| 自媒体 | 待发布 2 条 | 自媒体页面 |

- 已完成或有内容的站点自动「点亮」（用强调色）；
- 每个站点是一张卡片，点击直接进入对应模块；
- 首页只做展示和跳转，不做编辑（编辑进对应模块）。

---

## 4. 每个模块的用途

### 4.1 今日计划
记录和管理「今天要做的事」，看完结进度。

### 4.2 AI实践（含两个子功能）
- **AI 使用日志**：记录每天用 AI 做了什么、结果如何、有什么心得，方便回看自己的使用轨迹；
- **提示词/提问模板库**：把常用好用的 AI 提问模板分类存好，随取随复制。

### 4.3 自媒体
管理内容从想法到发布的完整过程，平台覆盖小红书（主阵地）、抖音/视频号、公众号，功能侧重小红书。

### 4.4 运动计划
以瑜伽/拉伸为主，制定每周运动计划（哪天、练什么、练多久）并每天打卡，看每周完成情况。

### 4.5 饮食计划
提前安排每天早/午/晚餐吃什么，当天照着吃，可以按整周规划。

---

## 5. 每个模块第一版最必要的数据和操作

### 5.1 今日计划
- 数据：日期、事项内容、时间（可选）、优先级（高/中/低，默认中）、备注（可选）、是否完成。
- 操作：新增事项；勾选完成/取消；编辑；删除（二次确认）；按日期切换查看（前一天/后一天/回到今天）。

### 5.2 AI实践 · 使用日志
- 数据：日期、用途（写代码/写文案/学习/其他）、所用工具（如 ChatGPT/Claude/Codex 等）、做了什么、结果与心得。
- 操作：新增日志；查看按日期倒序的列表；按用途/工具筛选；编辑；删除（二次确认）。

### 5.3 AI实践 · 提示词模板库
- 数据：分类标签（写作/编程/学习/生活/其他）、标题、提示词内容、适用场景（可选）、效果备注（可选）。
- 操作：新增模板；按分类筛选；一键复制内容到剪贴板；编辑；删除（二次确认）。

### 5.4 自媒体
- 数据：
  - 灵感：内容、平台（小红书/抖音/视频号/公众号）、状态（灵感/已转内容）、记录日期；
  - 内容：平台、标题、状态（灵感 → 撰写 → 待发布 → 已发布）、链接（可选）、发布日期（可选）、备注（可选）。
- 操作：
  - 灵感库：新增灵感、标记平台、转成内容、删除（二次确认）；
  - 内容管理：新增内容、编辑、推进状态（灵感→撰写→待发布→已发布，可回退）、按平台筛选、删除（二次确认）；
  - 顶部统计：本月各平台发布数、各状态数量。

### 5.5 运动计划
- 数据：
  - 每周计划：周一到周日各天的运动内容、时长（按周模板维护）；
  - 打卡记录：日期、是否完成、额外记录（今天额外练了什么，可选）。
- 操作：
  - 编辑本周计划（哪天、练什么、练多久）；
  - 按日期打卡/取消打卡；
  - 查看本周完成统计（练了 x 天 / 计划 y 天）。

### 5.6 饮食计划
- 数据：日期 × 三餐（早/午/晚）的吃什么内容、每餐是否吃完（打勾）。
- 操作：
  - 按天填写三餐内容；
  - 按整周查看/编辑 7 天 × 三餐；
  - 复制某天安排到另一天（如「复制昨天」）；
  - 当天吃完了打勾。

---

## 6. 首页、今日计划和各模块之间的关系

- **首页是入口和汇总**：只读展示今天各模块的一句话摘要，点击进入对应模块；
- **今日计划是独立模块**：管理当天待办，同时是首页「今日计划」站点的数据来源；
- **每个模块的数据是唯一事实来源**：首页摘要、统计数字都从对应模块的实时数据计算得出，不在首页单独存一份；
- **今日路线顺序**即一天的顺序：计划 → 三餐 → 运动 → AI → 内容，站点按此排序展示。

---

## 7. 本地数据文件、备份和恢复要求

### 7.1 数据存储
- 所有数据保存在 App 所在文件夹的 `data/` 目录中，以独立文件形式存放（JSON 文件）；
- 数据文件结构清晰、可直接查看；用户可以像普通文件一样复制、保存；
- 任何修改即时写入文件，刷新、关闭浏览器、重启电脑后数据不丢失。

### 7.2 备份
- 「设置」中提供一键「导出备份」：生成一个 `.json` 备份文件并下载/保存到电脑；
- 备份文件包含全部模块数据，可独立保存到任意位置。

### 7.3 恢复
- 「设置」中提供「恢复备份」：选择备份文件导入；
- 导入前明确提示「导入会覆盖当前全部数据」，确认后才执行；
- 导入成功后界面刷新为备份时的数据。

### 7.4 启动方式
- 双击启动器（macOS `.command` 文件）→ 自动启动本地服务并打开浏览器进入 App；
- 若端口被占用，自动换用可用端口；
- 关闭浏览器不影响数据（数据已在文件中）。

---

## 8. 第一版必须完成的功能

1. 双击启动器自动打开浏览器进入 App；
2. 左侧导航在 6 个板块间切换；
3. 首页：日期、快捷备忘（增删）、今日路线 5 个站点摘要与跳转、站点点亮；
4. 今日计划：增删改、勾选完成、优先级、按日期切换、进度统计；
5. AI实践：使用日志（增删改、列表、筛选）+ 提示词模板库（增删改、分类、复制）；
6. 自媒体：灵感库 + 内容管理（状态流转、平台筛选、本月统计）；
7. 运动计划：每周计划编辑 + 每日打卡 + 本周统计；
8. 饮食计划：按天/整周安排三餐 + 复制安排 + 吃完打勾；
9. 所有改动即时自动保存，操作提示「已保存」；
10. 删除操作二次确认；
11. 数据落盘为 `data/` 下的独立文件；
12. 一键导出备份、导入恢复（导入前提示覆盖）。

---

## 9. 第一版暂时不做的功能

- 手机端、联网、登录、云同步；
- 复杂统计图表（第一版只用简单数字统计）；
- 闹钟/到点提醒、消息推送；
- 与苹果日历、微信等外部应用联动；
- 多台电脑同步；
- 导入/导出 Excel；
- 图片、视频、文件上传管理（第一版只记录文字信息）；
- 拖拽排序（先用上移/下移或按时间排序）；
- 多用户/多账号。

---

## 10. 可实际检查的验收标准（AC）

### 10.1 启动与持久化
- [ ] 双击启动器后，浏览器自动打开并进入首页，无需手动输入地址；
- [ ] 新增/修改/删除任何数据后刷新页面，数据不丢失；
- [ ] 关闭浏览器、退出后重新双击启动，所有数据仍在；
- [ ] `data/` 目录下存在独立的数据文件，可直接用文本编辑器打开查看。

### 10.2 首页
- [ ] 首页显示当前日期；
- [ ] 快捷备忘可新增、可删除，刷新后仍在；
- [ ] 今日路线 5 个站点摘要与实际数据一致；
- [ ] 点击任一站点可跳转到对应模块；
- [ ] 已完成/有内容的站点正确点亮。

### 10.3 今日计划
- [ ] 可新增事项，填时间、优先级、备注；
- [ ] 可勾选完成/取消；
- [ ] 可编辑、可删除（删除有二次确认）；
- [ ] 可切换日期查看历史计划，进度数字正确。

### 10.4 AI实践
- [ ] 使用日志可新增、编辑、删除，按日期倒序展示，可按用途/工具筛选；
- [ ] 提示词模板可新增、编辑、删除，可按分类筛选；
- [ ] 模板「复制」按钮能把内容复制到剪贴板。

### 10.5 自媒体
- [ ] 灵感库可新增、标记平台、删除；
- [ ] 内容可新增、编辑、删除；
- [ ] 内容状态可推进（灵感→撰写→待发布→已发布）也可回退；
- [ ] 可按平台筛选；顶部本月统计数字与数据一致。

### 10.6 运动计划
- [ ] 可编辑每周计划（哪天、练什么、练多久）；
- [ ] 可对当天打卡/取消打卡；
- [ ] 本周统计（练了 x 天 / 计划 y 天）数字正确。

### 10.7 饮食计划
- [ ] 可按天填写三餐、按整周查看；
- [ ] 可把某天安排复制到另一天；
- [ ] 三餐可打勾标记吃完，刷新后状态保留。

### 10.8 备份与恢复
- [ ] 一键导出可生成 `.json` 备份文件；
- [ ] 修改若干数据后，用旧备份恢复，数据回到备份时的状态；
- [ ] 恢复前有明确覆盖提示，需确认后才执行。

---

## 附：变更记录
| 版本 | 日期 | 说明 |
| --- | --- | --- |
| v1.0 | 2026-08-25 | 初版，汇总需求确认与页面/交互设计方案 |
